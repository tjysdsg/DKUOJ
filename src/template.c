#include <memory.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    while (scanf("%d", &n) != EOF) {
        int i = 0;
        for (; i < n; i++) {
            int a, b;
            scanf("%d %d", &a, &b);
        }
    }
    return 0;
}
